# 🚀 راهنمای امکانات جدید Telegram Admin Pro v3.0

## 📋 خلاصه بهبودها

این نسخه شامل بهبودهای مهمی است که دقت و کارایی سیستم را به‌طور قابل توجهی افزایش می‌دهد:

### ✅ امکانات جدید

#### 1. **نمایش آمار زنده (Live Statistics)**
- 📊 **چت‌های خوانده نشده**: تعداد لحظه‌ای چت‌های نیازمند پاسخ
- 📤 **پیام‌های ارسالی**: شمارش کل پیام‌های ارسال شده در جلسه
- 🎯 **درصد موفقیت اسکن**: نرخ موفقیت در تشخیص چت‌ها
- ⏰ **زمان آخرین اسکن**: نمایش زمان دقیق آخرین بررسی
- 🔧 **روش اسکن فعال**: Vision AI یا OCR

#### 2. **دکمه‌های تست اتصال**
- 🤖 **تست Vision AI**: بررسی اتصال به Ollama و مدل llava
- 📱 **تست تلگرام**: بررسی امکان گرفتن اسکرین‌شات
- 🔍 **تست اسکن چت**: آزمایش کامل سیستم تشخیص

#### 3. **سیستم هیبرید Vision AI + OCR**
- 🧠 **تشخیص اولیه با Vision AI**: تحلیل هوشمند با مدل llava
- 👁️ **Fallback به OCR**: استفاده از OCR در صورت عدم دسترسی به AI
- 🎨 **تشخیص رنگی**: شناسایی نقاط سبز/آبی نشان‌دهنده پیام‌های جدید
- 📝 **تحلیل متنی**: تشخیص کلمات کلیدی مرتبط با پیام‌های خوانده نشده

## 🛠️ تنظیمات فنی

### کتابخانه‌های جدید
```
pytesseract==0.3.10     # OCR engine
easyocr==1.7.0          # Advanced OCR with deep learning
opencv-python==4.12.0.88 # Computer vision
numpy==2.2.6            # Numerical computations
torch                   # Deep learning framework
torchvision             # Computer vision models
```

### پیش‌نیازها
1. **Tesseract OCR** (اختیاری - برای pytesseract)
2. **Ollama** (برای Vision AI)
3. **مدل llava** (برای تحلیل تصویر)

## 🔧 نحوه استفاده

### راه‌اندازی اولیه
1. کتابخانه‌ها را نصب کنید:
   ```bash
   pip install -r requirements.txt
   ```

2. برنامه را اجرا کنید:
   ```bash
   python telegram_admin_pro.py
   ```

3. در اولین اجرا، مدل EasyOCR دانلود می‌شود (یکبار)

### استفاده از آمار زنده
- تب **"مانیتورینگ"** را باز کنید
- آمار به‌صورت خودکار به‌روزرسانی می‌شود
- برای ریست آمار، دکمه **"ریست آمار"** را بزنید

### تست اتصالات
- **تست Vision AI**: بررسی اتصال به Ollama
- **تست تلگرام**: تأیید امکان اسکرین‌شات
- **تست اسکن چت**: آزمایش کامل سیستم

## 🎯 مزایای سیستم هیبرید

### پایداری بالا
- اگر Vision AI در دسترس نباشد، OCR جایگزین می‌شود
- عدم وابستگی کامل به یک روش تشخیص

### دقت بهتر
- ترکیب دو روش مختلف برای تأیید نتایج
- تشخیص بهتر نشانه‌های بصری پیام‌های خوانده نشده

### مانیتورینگ دقیق
- ثبت جزئیات هر مرحله اسکن
- آمار عملکرد برای بهینه‌سازی

## 🔍 عیب‌یابی

### مشکلات رایج

#### OCR کار نمی‌کند
- مطمئن شوید pytesseract و easyocr نصب شده‌اند
- در صورت نیاز، Tesseract OCR را جداگانه نصب کنید

#### Vision AI در دسترس نیست
- Ollama را راه‌اندازی کنید
- مدل llava را نصب کنید: `ollama pull llava`

#### تشخیص چت‌های خوانده نشده ضعیف
- از دکمه‌های تست برای بررسی اتصالات استفاده کنید
- لاگ‌ها را بررسی کنید: فایل `admin.log`

### لاگ‌های مفید
```
📊 OCR یافت: 5 چت، 2 خوانده نشده
🤖 Vision AI متصل است
⚠️ OCR در دسترس نیست - از Vision AI استفاده می‌شود
```

## 📈 آمار عملکرد

### معیارهای اندازه‌گیری
- **Total Scans**: تعداد کل اسکن‌های انجام شده
- **Success Rate**: درصد موفقیت در تشخیص چت‌ها
- **Unread Found**: تعداد چت‌های خوانده نشده یافت شده
- **Method Used**: روش تشخیص استفاده شده (Vision AI / OCR)

### بهینه‌سازی
- برای سرعت بیشتر، از GPU استفاده کنید
- برای دقت بالاتر، تنظیمات تشخیص رنگ را تنظیم کنید
- برای پایداری، هر دو سیستم را فعال نگه دارید

## 🔮 امکانات آینده

- تشخیص خودکار نوع چت (شخصی/گروه)
- تحلیل محتوای پیام‌ها
- پیش‌بینی اولویت پاسخ‌دهی
- تنظیمات پیشرفته OCR
- پشتیبانی از زبان‌های مختلف

---

**نکته**: این ویژگی‌ها در نسخه Pro موجود هستند و نیاز به لایسنس معتبر دارند.
